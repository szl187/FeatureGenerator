22/7/26
增加默认在输出文件首行附加特征说明，使用-noheader取消
增加包原始序号和相对时间的输出
修正包间隔特征不断增大的错误
完善分割“连续下行包序列”的机制
	-以大时间间隔分割版本，同个-segint X将大于X被平均间隔的时间间隔作为分割下行序列的标准，X默认为3。并取消特征请求间隔riu，以段间隔代替（分割结果应满足每段平均包数和段平均间隔较为稳定）
	-以上行包分割版本机制与之前相同
22/7/18
修改了四个特征的计算方式使其更适合实际。使用-sd（segment decay）参数则用原先的代替。
新增了两个特征，但是根据这边的分析结果，新增特征重要性都较低。使用-lite参数可以取消输出这两个
程序在命令行输出特征名的list，可以直接拷贝到python文件里面用
新增一些python小工具
22/7/13
main.cpp  规范书写、注释、缩进；参数化输入；新增文件夹读取模式； 使用io.h库函数组合取代getcwd，需要在windows系统运行。
flow.h 新增外部变量引用
flow.cpp  规范书写、缩进
ReadMe.md 根据代码功能进行调整
